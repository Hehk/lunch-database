type User {
  id: ID! @unique
  name: String!
  slackId: String!
  orders: [Order!]!
}

type Restaurant {
  id: ID! @unique
  name: String!
  sourceId: ID!
  location: String!
}

type Order {
  id: ID! @unique
  content: String!
  user: User!
  restaurant: Restaurant!
}
